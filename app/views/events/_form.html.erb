<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artista %><br>
    <%= f.text_field :artist %>
  </div>
  <div class="field">
    <%= f.label :descrição %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label "Preços à partir de" %><br>
    <%= f.text_field :price_low %>
  </div>
  <div class="field">
    <%= f.label :até %><br>
    <%= f.text_field :price_high %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.date_select :event_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
